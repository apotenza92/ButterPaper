# Performance Smoke Tests

This directory is reserved for deterministic perf checks:
- first-page render time proxy
- scroll throughput proxy
- memory growth guardrails

Perf gates will be added incrementally as viewer-core rendering pipeline lands.
