# UI Tests

Cross-platform headless UI tests live in crate integration tests (for example GPUI tests under `crates/gpui-app` using `#[gpui::test]`).

This directory is reserved for future workspace-level UI suites.
